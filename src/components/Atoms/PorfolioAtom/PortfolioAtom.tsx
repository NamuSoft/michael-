import React from 'react'

type Props = {
    name: string,
    image: string,
    type: "Branding" | "Mockup",
}

export default function PortfolioAtom(props: Props) {
  return (
    <div>PortfolioAtom</div>
  )
}
